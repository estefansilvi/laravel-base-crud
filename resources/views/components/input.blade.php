<div class="form-group">
    <label for="{{$id}}">{{$label}} </label>
    <input type="{{$type}}" name="{{$name}}" class="form-control" id="{{$id}}" aria-describedby="{{$aria}}" placeholder="{{$placeholder}}">
    <small id="{{$aria}}" class="form-text text-muted"></small>
  </div>
